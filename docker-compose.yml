version: '3.8'

services:
  backend:
    build: .
    ports:
      - "11013:11013"
    environment:
      - MCP_PORT=11013
      - MCP_HOST=0.0.0.0
      - LOG_LEVEL=INFO
    volumes:
      - ./src:/app/src
    command: uvicorn tahoma2d_mcp.server:asgi_app --host 0.0.0.0 --port 11013 --reload
